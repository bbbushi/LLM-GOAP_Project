# 开发日志 2026-09-08

## 今日概览
项目从空仓库进入可交接状态：9-7 晚完成初始化提交与 GOAP 教程文档，9-8 凌晨落地 /checkpoint 检查点工作流并建立 HANDOFF.md，上午加入模型路由约束；随后把约束正文从 CLAUDE.md 拆分到 AGENT_COMMON.md，并搭起每晚自动开发日志机制（当日已试运行，本篇为 --force 重跑产物）。M1 GOAP 内核代码尚未动工。

## 变更明细
- 项目初始化（890c341，9-7）：Unity 2022.3.15f1c1 工程骨架 + 权威设计文档 Docs/DESIGN.md v0.4（LLM 规则生成 + GOAP 双层架构）。
- GOAP 教程（e1e5ac4，9-7）：新增 Docs/GOAP.md，面向非游戏开发协作者，以 STRIPS/A*（状态图上的 A*）讲解，附对齐 M1 接口的无头 C# 最小实现；同步修正 .vscode 的 sln 指向。
- 检查点工作流（caac280，9-8 凌晨）：新增 .claude/skills/checkpoint/ 与 HANDOFF.md，确立「完整检查点 = 更新 HANDOFF + 同一提交；轻量打点 = 仅提交」。
- 约束与依赖（d11d4a3，9-8 上午）：CLAUDE.md 加入模型路由表（视觉/UI→GPT-6、架构设计→glm-5.3、文书/测试→glm-5.3-flash 默认）；Packages/manifest.json 将 collab-proxy 替换为本地安装的 MCP For Unity 包。
- 未提交：CLAUDE.md 瘦身为纯指针文件，约束正文迁至新文档 Docs/AGENT_COMMON.md（9 节，含工程约定、新平台接入指引）；新增 Scripts/devlog/ 自动日志机制（launchd plist + devlog.sh + PROMPT.md + README.md），每晚 21:40 无头扫描仓库生成当日日志，pathspec 单独提交、幂等跳过、支持 --force 补跑。

## 状态与风险
- 处于 M1 动工前：核心接口（IAction / IGoal / IWorldState / IPlanner / IRuleProvider / ILLMProvider）与 World State JSON Schema 未定义，Assets/script/ 仅占位 Test.cs。
- 风险：d11d4a3 已把 manifest.json 中的本机绝对路径（file:D:/Unity/...）带入版本库，推送共享远程前必须处理，否则协作者无法解析——HANDOFF.md「进行中」仍记其为未提交，已过时。
- Scripts/devlog/ 的脚本与 plist 内含本机绝对路径（含外置卷前缀），README 已注明换机器需同步改两处。
- 协作侧：本地分支已关联 origin，共享远程协作流程未定；约定检查点不做测试/编译验证（明知破坏编译会先提醒）。

## 明日建议
- 处理 MCP 包绝对路径：改为双方可用的安装方式，或将 manifest.json / packages-lock.json 从共享提交中撤出。
- 启动 M1：定义核心接口与 World State JSON Schema（范围见 DESIGN.md §6）。
- 为未提交内容（AGENT_COMMON.md 拆分、Scripts/devlog/）打一次检查点落库。
